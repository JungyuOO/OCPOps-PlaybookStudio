# Concept Manifests

This folder contains concept synonym and taxonomy manifests used to normalize
OpenShift terms for search, metadata extraction, and evaluation.

These files are control data, not runtime truth. Runtime behavior should be
verified through PostgreSQL/Qdrant payloads and evaluation cases after import or
backfill.
