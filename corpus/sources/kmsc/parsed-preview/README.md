# Parsed Preview Packages

This folder contains generated preview packages from customer source material.

These packages are allowed to include derived chunks, image assets, and learning
manifests for inspection before or after database import.
